import Home from '../containers/Home';
import Details from '../containers/Details';

const screenMap = {
  HomeScreen: {
    screen: Home,
  },
  DetailScreen: {
    screen: Details,
  },
};

export default screenMap;
